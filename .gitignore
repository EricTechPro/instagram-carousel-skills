# transient generation output (top-level only — committed example outputs are kept)
/output/
/carousel/
*.tmp

# intermediate render artifacts (we commit the final slide-NN.png, not these)
*.plate.png
_generate.log

# python
__pycache__/
*.pyc
venv/
.venv/

# os
.DS_Store
