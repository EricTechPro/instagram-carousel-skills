# Used by the generate skill to crop generated plates to exactly 1080x1350
# and to build the contact-sheet montage. No text rendering (slides are
# fully generated by GPT Image 2 in the default pipeline).
Pillow>=10.0.0
