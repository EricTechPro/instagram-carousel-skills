# Pillow drives the hybrid render: it crops each generated background plate to exactly
# 1080x1350 and overlays all text + the real logo PNGs (the image model renders only the
# text-free world). Also used to build the contact-sheet montage.
Pillow>=10.0.0
