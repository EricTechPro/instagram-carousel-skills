---
description: Generate carousel slides from an approved carousel-spec.md (HiggsField + Pillow → 1080x1350 PNGs)
---

Invoke the **instagram-carousel-generate** skill to render an approved spec into slides.

Spec to render (a path to `carousel-spec.md`, or blank to use the most recent one):

$ARGUMENTS

Follow the skill: check prereqs, show a cost estimate and get confirmation before any spend, then
render the slides at 1080×1350. To write or edit the copy first, use `/instagram-carousel-plan`.
